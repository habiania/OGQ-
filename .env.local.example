# 둘 중 하나 이상 채우면 됩니다. UI에서 OpenAI/Gemini 중 선택해서 사용.
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=...

# 기본 제공자 (openai | gemini)
DEFAULT_IMAGE_PROVIDER=openai

# 암호 잠금(배포 시). 둘 다 채우면 사이트 전체가 기본 인증으로 잠김.
# 로컬에서 비워두면 인증 없이 통과.
BASIC_AUTH_USER=
BASIC_AUTH_PASS=
